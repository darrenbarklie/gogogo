package iteration

// Repeat takes characters to repeat and a integer for number of repetitions.
func Repeat(character string, repeatCount int) string {
	var repeated string
	for i := 0; i < repeatCount; i++ {
		repeated += character
	}
	return repeated
}
